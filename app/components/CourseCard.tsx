"use client";
import React, { ReactNode } from "react";

type Props = {
  courseCategory: Array<Object>;
};

const CourseCard = ({ courseCategory }: Props): ReactNode => {
  return;
  <div></div>;
};

export default CourseCard;
